<!-- Validation failure messages -->
<% if @meal_time.errors.any? %>
  <% @meal_time.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Meal Time</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_meal_time" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for time -->
      <div class="form-group">
        <label for="time" class="control-label">
          Time
        </label>

        <input type="text" id="time" name="time" placeholder="Time" class="form-control" value="<%= @meal_time.time %>">
      </div>

      <button class="btn btn-success">
        Create Meal Time
      </button>
      or
      <a href="/meal_times">Cancel</a>
    </form>
  </div>
</div>
